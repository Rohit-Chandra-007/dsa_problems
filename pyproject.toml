[project]
name = "dsa-problems"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.basedpyright]
reportCallIssue = "none"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
